import { PlaylistCity } from '../Playlist/Playlist.types';

export type FilterablePlaylistsComponentProps = FilterablePlaylistsProps;

export type FilterablePlaylistsProps = {
  playlists: PlaylistCity[];
};
